# Project Index

Feature lifecycle manifest. States: `backlog` → `active` → `done`; `archived` for abandoned work (physically moved to `.project/archive/`). Paths never change with state — only this index does. Release agent keeps it current.

| Feature | State | Since | Notes |
|---|---|---|---|
| example | backlog | 2026-07-11 | placeholder demonstrating structure |
